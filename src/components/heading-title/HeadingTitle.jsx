import "./heading-title.css";
const HeadingTitle = ({ title }) => {
  return (
    <div className="heading-style-for-div">
      <h2 className="heading-style-for-h2">{title}</h2>
    </div>
  );
};

export default HeadingTitle;
